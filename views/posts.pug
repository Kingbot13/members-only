extends layout 

block content 
    h1 View or create posts 
    form(method="" action='')
        label Title: 
            input#title(type='text' name='title')
        label Write Post 
            textarea#post(name="post")
        input#user.hidden(type='hidden' name='user' value=user.id)
        button Submit 
    hr
    if posts
        ul 
            each post in posts 
                li 
                    if user.membership 
                        div #{post.user.firstName} #{post.user.lastName}
                        div #{post.date} 
                    div #{post.content}         
    else 
        h2 Be the first to write a post!